---
twocolumn: true
---

# Mermaid Invalid Fixture

This fixture validates graceful fallback when Mermaid syntax is invalid.

```mermaid
this is not valid mermaid syntax and should fail rendering
```
